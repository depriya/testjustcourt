This repo contains:
* `env.ARM.template.json`: A parameterized ARM template for deploying an Azure virtual machine based on an image from marketplace (and all necessary networking resources).
  * The parameter definitions in this file contain metadata explaining what they are used for.
* `environment.yaml`: A yaml file that provides metadata about the environment definition.
